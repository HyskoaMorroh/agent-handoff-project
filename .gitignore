__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.venv-*/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/

# 图文说明的包内副本。真源是 docs/guide.html（由 scripts/build_guide.py 生成）；
# 这一份只在打包前生成，好让 wheel 也带上说明文档。两份都进 git 就成了两个
# 真源，改一处忘另一处必然漂移。
src/agent_handoff/gui/static/guide.html

# graphify 的图谱产物（第三方工具生成，可随时重建）。
graphify-out/

# 交接文件的同日备份。它是「同一天重跑且内容变了」时留下的救生索，
# 针对的正是「用了 --no-commit 所以 git 里没有上一份」这个缺口——
# 让它自己进 git 就本末倒置了，而且会给每次重跑加一个噪声文件。
# 真正的历史归 git 管：提交过的交接文件在历史里都能翻出来。
*.prev.md

# 这个工具**自己跑自己**产生的交接记录。
#
# 为什么不进 git：交接文件的内容是「上一段对话的现场」，里面照抄用户的原话、
# 引用的外部项目路径、以及当时正在做的**其它**项目的需求原文。实测本仓库
# 曾提交过一份 1.6 MB / 53013 行的交接记录，里面是另一个项目的完整需求书与
# 桌面目录结构——那不是这个仓库的必备文件，公开出去也不合适。
#
# 用 `*-handoff.md` 而不是逐个列文件名：交接文件名只带日期，明天再跑就是
# 一个新名字，逐个列必然漏。代价是本仓库自己跑 `agent-handoff .` 时，
# 产物不会被 `git add -A` 带进快照提交——那正是想要的效果。这条规则只作用于
# 本仓库；对**用户自己的**仓库没有任何影响，那边的交接文件照旧会被提交。
#
# 三份历史交接记录（2026-08-22 / 08-23 / 08-24）已在 2.8.2 里移出跟踪。
# 它们仍在 git 历史里，需要时能翻出来。
#
# 例外：**方案书**留着（`docs/2026-08-25-*-plan.md`）。那是 CHANGELOG 里
# 「为什么这么做」的证据链，且内容只关于本项目。
*-handoff.md

# 勘查笔记与临时调研记录：本机路径、截图目录、外部项目名，都不该公开。
docs/superpowers/
