# Changelog

本文件记录对用户可见的变化。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.4.0] - 2026-08-25

### 修复

- **一周以前的 Codex 会话在工具里完全不存在。** 列表看起来正常，只是短了一截，
  用户没有任何线索知道自己漏掉了什么——这是最坏的一种失效：沉默的。

  根因：Codex 会把 **7 天前**的 rollout 原地 zstd 压缩成 `.jsonl.zst`（level 3，
  压缩期间用 `rollout-compression.lock` 加锁）。而会话发现只匹配 `.jsonl`，
  全仓没有一处认识 `.zst`。而且它会越来越严重：每天都有新的会话跨过 7 天线。

  现在两种扩展名一视同仁，读取时按扩展名挑解压流。**不引入运行时依赖**（零依赖
  是刻意取舍：工具运行在刚崩掉的环境里，任何要 pip 装的东西都是一条失败路径），
  只利用恰好装好的实现，按可信度依次尝试 `compression.zstd`（Python 3.14 标准库）、
  `zstandard`、`pyzstd`。三个都没有时会话**仍然出现在列表里**，标注「读不到正文」
  并说明装哪一个能读到——从「假装它不存在」变成「可见且有明确原因」。

- **Codex 的上下文占用漏算了输出与推理 token。** 取的是
  `last_token_usage.input_tokens`，而 Codex 自己显示占用率时走
  `TokenUsage::tokens_in_context_window()`，实现是 `self.total_tokens`。
  `TokenUsage` 有六个字段（input / cached_input / cache_write_input / output /
  reasoning_output / total），只取 input 会把输出与推理全漏掉——**推理密集的
  会话因此被系统性低估**，而这类会话恰恰是最需要交接的。

  现在取 `total_tokens`，缺失时（老转录可能不写）才退回 `input_tokens`。
  另一半判断保持不变且已加测试钉住：仍然用 `last_token_usage` 而不是
  `total_token_usage`——后者是全会话累计，会远超窗口，当占用用会永远判成爆满。

- **压缩归档的会话会被体积判据判成「健康」。** zstd level 3 对 JSONL 通常压到
  十分之一上下：一个 27 MB 的爆满会话在盘上只剩 2 MB 多，体积阈值会说「留意」
  甚至「健康」——正好把最该交接的会话判成最不需要交接的。

  新增 `unknown` 区间：读不到正文时如实说不知道，不拿压缩后的体积去对照未压缩
  的阈值。排序上它落在 `watch` 与 `ok` 之间——不算健康（可能正是最该交接的那个），
  也不顶到 `critical` 前面挤掉有真实证据的行。界面用中性灰而不是任何语义色：
  绿色会被读成健康，红色会被读成出事，两种误读都比不上如实说不知道。

- **压缩转录的会话 ID 尾巴上挂着 `.jsonl`。** `Path.stem` 只剥最后一层扩展名，
  `xxx.jsonl.zst` 剥成 `xxx.jsonl`——那个字符串既不是会话 ID 也不是文件名，
  拼进 `codex resume` 必然报「找不到会话」。两处（身份提取与兜底）都改成两层都剥。

- **交接文件与计划文档是非原子写。** `write_bytes` 的语义是「截断到 0，然后写」，
  这两步之间读它的人看到的是空文件或半截文档——而这份文档存在的全部意义就是
  「上一个会话死了，现场在这里」。现场文档自己在交付途中被截断，是最不能接受的
  失败方式。触发条件也不稀奇：磁盘满、进程被杀、两个会话同时跑同一条命令
  （并发检测只警告不阻断，所以这是允许发生的）。

  改为同目录唯一临时文件（`O_EXCL` + pid + 计数器，撞名重试 16 次）→ 写完
  `flush` + `fsync` → `os.replace` 原子替换 → POSIX 上再 fsync 父目录让目录项
  也落盘。失败路径一律删掉临时文件，不在用户仓库里留垃圾；写不成一定抛出去，
  静默失败比崩掉更坏。计划文档尤其需要这个保护：它是**用户手写**的，而且
  往往一份副本都没有（交接文件至少还有 `.prev`）。

- **仓库根目录下的点文件被并发检测漏掉。** `.env`、`.gitignore`、
  `.claude/settings.json` 的并发改动不会进提示。根因是 `detect_concurrency`
  里仍在用 `str.lstrip("./")`——`lstrip` 收的是**字符集合**而不是前缀，会把开头
  所有的 `.` 和 `/` 逐个剥掉：`./.env` 变成 `env`，那个名字在 `changed_paths()`
  的集合里不存在，于是文件被静默跳过。凭据类文件几乎全是点文件，而「另一个
  会话正在动 .env」正是最该被提示的一种并发。修正为按前缀剥（复用同文件里
  已有的 `_strip_leading_dotslash`，此前只在另一处调用点用上了）。

- **经由符号链接进入仓库时并发检测整个中断。** `os.walk` 给出的 root 是解析后的
  真实路径（macOS 的 `/tmp` → `/private/tmp` 是最常见的一种），与传进来的 repo
  不同前缀，`relative_to` 抛 `ValueError` 且此处无守卫。并发检测是防止两个会话
  互相覆盖的那道闸，一个异常让它整个不工作，比少一条提示危险得多。

- **扫描缓存在线程池下无锁。** `scan_session_vitals` 用 `ThreadPoolExecutor`
  并发扫描，多个线程同时进 `_cached_scan`。GIL 保证单次 `__setitem__` 不撕裂，
  但「查 → move_to_end → 写 → 循环 popitem」是四步组合，线程在中间被切走时
  `len()` 判断基于过期视图：两边都以为不必逐出，缓存于是越过 256 上限；反过来
  也可能把对方刚写进去的热条目当成最旧的挤掉。加锁只圈住这几步字典操作，
  读文件（可能上百 MB）留在锁外，所以并发度不受影响。

- **`CODEX_SESSIONS_ROOT` 没被读取。** Codex 的会话路径解析顺序是
  `CODEX_SESSIONS_ROOT` → `CODEX_HOME/sessions` → `~/.codex/sessions`，
  第一个直接指向**会话目录本身**、比 `CODEX_HOME` 更靠前，且不能再往上拼
  `sessions` 子目录。不读它的后果与不读 `CODEX_HOME` 一样：一个转录都找不到。

- **多个会话一起交接时，深度链接会串到别的会话。** 勾三个会话，第三条的
  `codex://threads/…` 指向第二条的 ID——点开是别人的对话，而链接本身语法合法，
  所以不报错，只是静默打开错的东西。

  根因：`deep_link()` 写成 `thread_id or session_id`，让 `thread_id` 优先。
  但 `thread_id` 是**派生来源**（这个会话从哪个线程 fork 出来），不是本会话身份。
  Codex 的派生会话里它指向父线程，于是链接跳到父会话。改为 `session_id` 优先，
  仅在它缺失时才退回 `thread_id`。实测 3 个会话的 ID 与深链现已一一对应。

- **会话归属取「正文里第一个提到的仓库」，而不是它实际在哪工作。** 一个会话
  提到别的项目是常态（审计外部参考、对比两个仓库），所以「先提到谁」不等于
  「在哪工作」。实测本项目的会话被归到了另一个仓库。

  现在优先用 `cwd` 上溯找 `.git`，找不到才回退到正文里验证过的仓库路径——
  沙箱容器目录、家目录这类非仓库 cwd 仍然走回退。

- **APP 类型判定在三处各写了一份。** `handoff.py` / `portable.py` /
  `transcript.py` 各自拼 `rollout-` 前缀判断，迟早分叉；判错会让续接命令给错。
  收拢成 `platform.agent_for` 单一判据。

- **Codex 会话导出时每条助手发言重复一次。** 体积白白翻倍，而新会话读到同一段
  叙述两遍，会以为那是两次独立的判断。

  根因：Codex 把同一条发言**同时**写进 `message` 与 `agent_message` 两种 payload
  （实测一份 rollout 里 message 1028 条、agent_message 807 条），解析器把两者都
  收了。现在按正文首 400 字符去重。实测 `**助手：**` 从 737 次降到 408 次。

- **`<thinking>` 块泄漏进导出的 Markdown。** 「不取 reasoning 类型」挡不住它——
  Codex 会把 `<thinking>…</thinking>` 直接拼在 `agent_message` 的正文里，
  实测泄漏 10 处。

  thinking 无签名不可回放，带过去只是占上下文的中间态文字；更糟的是它常包含
  「先试 A 再试 B」这类**已被放弃的路线**，新会话照着读会重走弯路。现在正文里
  的内嵌块也剥掉，没有闭合标签的半截（流式中断留下的）同样剥。

- **用户诉求被过滤过度。** 样板过滤把一部分真实提问一起挡掉了，实测同一个会话
  只剩 5 条用户轮。修去重与剥壳的先后顺序后恢复到 13 条。

- **超限截断会切掉结尾的当前进度。** 原先是 `out[:keep]` 从头切一刀，而**结尾
  恰恰是「上一个会话停在哪」——交接最需要的一句**。实测一个 3006 字符的会话被
  切到 2760 时，最后那条结论整条消失，新会话于是不知道做到哪了。

  现在**保头保尾、从中间丢**：三分之一的余量留给结尾（目标几段话就说完，
  而结尾要交代刚做完什么、下一步是什么，通常更长）。文案也跟着改了——
  原先说「只有前 N 字符」，现在说「中间已省略，头尾都保留」。

### 新增

- **多会话汇总的提示词里，每个会话都带齐六项定位信息。** 缺一样都会让接续会话
  卡住：

  | 项 | 用途 |
  |---|---|
  | 话题 | 认得出是哪一段工作 |
  | 转录路径 | 去读原文 |
  | 会话 ID | 喂给 `--find`、贴进 issue |
  | 工作目录 | cd 到对的地方——多个会话可能在不同子目录里跑过，只给仓库根不够 |
  | 续接命令 | 无损回去，**首选路径** |
  | 包内四件套位置 | 读完整对话（`sessions/<id>/session.md`） |

  四件套位置**按会话 ID 直接拼**，不等导出结果回填：提示词在打包之前就生成好了，
  等 `artifacts_dir` 是等不到的（那个字段只存在于包的 manifest 里，这一行会永不
  显示）。路径形态 `sessions/<id>/` 是固定契约，所以拼出来的一定对；文案里写明
  「若已打包带走」这个前提，没打包的人不会去找一个不存在的目录。

- **压缩边界会在导出里标出来。** 跨过这条线，之前的原始历史已被摘要取代——
  新会话据此知道「更早的细节不在这份文档里，**也不在原会话里了**」，
  而不是以为工具漏抄了。实测一份转录有 139 个 `context_compacted` 事件。

  这一点是对照 VSCode 的 Codex 导出补的：它有 `Context compaction / Source:
  automatic` 标记，本项目此前 0 处。

  实现上有个坑：`compact` 轮没有正文（Codex 只给 `source`），所以 `Turn.is_empty`
  必须为它开例外，否则渲染循环开头的空轮过滤会把 139 个边界全部吃掉——实测如此。

- **工具调用折叠块的 summary 带上了次数。** 折叠起来之后读者只看得见那一行，
  「7 次工具调用」比「工具调用」多告诉他这一段有多重。取自 VSCode 导出的
  `Called 5 tools`。

- **每个会话都能单独导出成可粘贴的对话，不再只有一段摘要。**

  此前交接只带会话自己写的压缩摘要（几百到几千字），新会话拿到的是「上一轮
  做了什么」的概述。现在每个被勾选的会话在包里有自己的目录，四样东西各回答
  一个问题：

  | 文件 | 回答什么 |
  |---|---|
  | `resume.txt` | 怎么**无损**回到这个会话（首选路径） |
  | `session.md` | 这个会话说过什么（可直接粘给新会话） |
  | `locate.txt` | 工作目录、会话 ID、深度链接 |
  | `meta.json` | 同一批信息的机器可读版本 |

  `session.md` **分级带**，不是全带也不是只带摘要：用户原话与助手结论按原文进，
  工具调用压成一行「名字 -> 结果摘要」，thinking / reasoning 不进。依据是实测的
  体积分布——一份 13452 行的 Codex rollout 里 `function_call` 与其输出占 6746 行
  （50%），而真正的对话消息只有 1614 行；子代理转录的「过程:报告」体积比是
  **124:1**。实测压缩比：30 MB 的 Claude 转录 → 95 KB，90 MB 的 Codex → 959 KB。

  超限时**先丢最早的工具摘要**再截人话，而且必须说出丢了多少：开头有目标与约束、
  结尾有当前进度，中间的过程最可丢。静默截断会让读者以为看到了全部，
  然后据此推断「这件事没发生过」。

  **说清楚边界：这仍然是有损的。** 模型内部状态与 prompt cache 是服务端加密的，
  换会话即失效；被否决方案的推理过程不可回放；工具授权与 MCP 连接是运行时状态。
  真正无损的只有原生 `--resume`——所以 `resume.txt` 排在第一个，
  `session.md` 里也把这条写在对话前面。

- **harness 注入的样板不再被当成用户原话。**

  `user` 角色在转录里是个**信道**而不是一个人：斜杠命令回显、后台任务通知、
  skill 正文、plugin 清单、系统提醒全都以它的身份出现。实测一份转录的 user 轮里
  `<command-name>` 出现 28 次、`<task-notification>` 14 次，而真实诉求只有 23 条。

  不挡的后果是导出的第一条「用户原话」是
  `<local-command-caveat>Caveat: The messages below were generated by...`——
  新会话据此推断意图，等于从一句样板开始理解整个项目。实测过滤后 116 条用户轮
  **全部**是真实诉求，残留样板归零。

  两处细节：标签按**名字**匹配而不是整串，因为 Codex 的
  `<codex_internal_context source="goal">` 带属性，写死尖括号一个都匹配不到
  （这一处实测漏了 24 次）；而这个标签里装的是用户自己设的目标，所以它走
  **剥壳保内容**那条路——丢壳、留正文、再对「每轮重复注入」去重。

- **每会话四项复制：工作目录 / 会话 ID / 深度链接 / 为 Markdown。**

  体检列表与找会话结果的每张卡片上都有。四样东西对应四个不同的下一步动作
  （cd 过去、贴进 issue、唤起 APP、粘给新会话），所以分开给而不是塞进一个
  「复制全部」。

  深度链接**只给真实存在的 scheme**：Codex 注册了 `codex://threads/<id>`，
  Claude Code 没有任何 URI scheme，所以那边不显示这个按钮。编一个不存在的
  `claude://…` 会让用户点了没反应，而他无从判断是链接错了还是 APP 没装——
  比不给更糟。边界与 `resume_cmd` 一致：外来转录与归档会话都不给，
  因为那两种情况下 APP 自己的索引里没有这条线程。

- **`--find` 支持一次给多个 ID / 关键词，且不再漏掉「不够新」的会话。**

  两个改动。一是多值：`--find a --find b,c` 与 `--find "a, b, c"` 等价，
  全角逗号与顿号都认——中文输入法下敲出来的是 `，` 和 `、`，
  只认半角会让粘进来的一串 ID 变成一个找不到的长字符串。

  二是修了一个真缺陷：原先按 `--limit` 扫最新若干个再在结果里搜，
  实测本机 467 个转录只覆盖 120 个（26%）——**握着正确的 ID 也有七成概率
  被告知「没找到」**，那是最坏的一种失败。现在 ID 片段走全量文件名定位
  （只读目录项，实测 43 毫秒），关键词搜索仍受 limit 约束（模糊查询本就该
  覆盖最近的若干个）。

  多个结果横跨不同项目时会**明确提示**：交接固化的是一个仓库的状态，
  把两个现场汇总进一份提示词，新会话会拿 A 项目的结论去改 B 项目的代码。
  提示而不拦——同一份工作跨两个仓库（前后端分离、主仓 + 插件仓）是真实场景。

- **深色主题下浏览器自己画的控件也跟着变色了。** 页面配好了色，滚动条、下拉箭头、
  复选框、文本框的插入符却仍然是浅色的——因为 CSS 里从来没声明 `color-scheme`。
  一行成本，覆盖所有原生 UI，没有任何 JS 参与。

  三种状态各声明一次（不是两种）：裸 `:root` 是 light，系统深色在媒体查询里、
  带 `:not([data-theme="light"])` 守卫让手动选浅色能压过系统，手动深色在
  `[data-theme="dark"]` 里。

  **CSS 此前完全没有测试。** 这类缺陷不报错、不影响布局，只能靠人打开界面才看得见。
  新增两条契约：三种主题状态都要有 `color-scheme`；每个颜色令牌都必须在裸 `:root`
  上有兜底定义——只定义在媒体查询里的颜色，在「系统浅色 + 手动选深色」这类组合下
  会缺失，表现成某个元素透明或继承到意外的颜色。

- **`--export-bundle` / `--import-bundle`：真正能带到另一台电脑的交接包。**

  此前交接文档里的转录路径是**这台机器上的位置**，不是内容。
  `~/.claude/projects/C--Users-devin-proj/<id>.jsonl` 这个路径里，目录名本身
  编码了源机的 cwd——换机器后那个目录不存在，新会话拿到路径也读不到东西。
  **只给路径不给内容，就是「换机必然失效」的根本原因**，而工具此前完全没有
  导出能力（`src/` 里 `zipfile|tarfile|export|bundle` 一个命中都没有）。

  包的布局：`manifest.json`（必有 `schema_version`）+ `handoff/`（交接文档与
  开场提示词）+ `transcripts/`（**勾选转录的副本**）。

  路径存成占位符（`{CLAUDE_ROOT}`、`{CODEX_ROOT}`、`{CODEX_ARCHIVED_ROOT}`），
  导入时**按目标机器自己的根重新解析**，不做字符串替换——目标机器的
  `CLAUDE_CONFIG_DIR` 可能指向完全不同的位置甚至另一个盘。实测：把
  `CLAUDE_CONFIG_DIR` 指到别处后，同一个包里的路径跟着新根走；本机没有对应根时
  不编造路径，只指向包内副本。

  `archived_sessions` 有自己的占位符。共用 `{CODEX_ROOT}` 会让归档转录在导入时
  被解析到 `sessions/` 下——路径语法合法所以不报错，只是**静默指向错误位置**。
  实测这个缺陷在开发中真的出现过：`archived_sessions/2026/rollout-a.jsonl` 被
  还原成 `sessions/2026/rollout-a.jsonl`。

  几条刻意的设计边界：
  - **转录副本不脱敏，但明确告知。** 交接文档与提示词过脱敏层（家目录、他机
    用户名、密钥形态），`transcripts/` 里是原样字节——副本的价值就是保真，
    脱过的转录作为「那段工作的记录」已经失真。代价是包里可能有用户粘进会话的
    密钥；所以 `manifest.json` 里有 `warning` 字段，命令行与网页界面在**真带了
    副本时**才提醒（没带却警告会训练用户忽略提示，比不提示更糟）。
  - **包默认写在仓库外**（`~/.agent-handoff/bundles/`）。里面有转录副本，而转录
    可能含用户粘进会话的任何东西；默认写进仓库等于默认交给 `git add -A`，
    那正是本工具在别处极力避免的事。
  - **导入是只读的**：不往 `~/.claude` / `~/.codex` 写任何东西。放不放由用户定
    ——那会改变对应 app 的会话列表。Claude Code 自 v2.1.205 起也明确禁止篡改
    会话转录文件。
  - **manifest 当外部输入对待**：`..`、绝对路径、指向包外的条目一律拒收；
    版本比自己新时明确引导升级而不是猜字段含义；`UnicodeDecodeError` 显式捕获
    （它的 MRO 是 `ValueError`，既不是 `OSError` 也不是 `JSONDecodeError`）。
  - **逐条报告成败**：一份坏转录不让其余的进不来；超限（默认 24 MB）的转录不带
    正文但保留路径与 id，用户可以自己单独拷。
  - manifest 键排序 + LF 写出：包会被放进 git，键序随机的 JSON 每次都显示成
    整文件改动。

- **README 说清了「判定粒度是 Task 而不是 Step」。** 这个语义边界此前完全没记录，
  而它会让人误以为工具勾错了：一个 Task 的全部 Step 一起勾或一起不勾，证据只来自
  `**Files:**` 与 `**Interfaces:**` 声明，**Step 文字里提到的文件名和符号不算证据**。

  实测场景：Task 声明了 `mod.py` + `alpha`，两者都存在，于是 Task 判定完成、
  它下面「定义 gamma」那一步也被勾上——而 `gamma` 并不存在。这不是判错，是
  `gamma` 从未进入证据集。粒度做在 Task 上是因为 Step 是自然语言，从「定义
  gamma」这句话里猜符号名必然出错，而误勾不可逆：勾掉的步骤会从待办里永久消失。

  三份 README 各加一节，给出可照抄的计划片段和两种改法（拆成独立 Task，
  或把符号写进 `**Interfaces:**`）。

- **CI 现在挡住版本与徽章漂移，测试作业也有超时上限。** 版本写在三处
  （`pyproject.toml`、`__init__.py`、三份 README 的徽章），此前全靠人手同步、
  没有任何检查。漂移的后果是 `--version`、guide 顶栏、README 徽章三者不一致，
  读者只会当成「文档不可信」——而那正是这个工具最不能背的名声。

  测试数徽章同理：实测它一直停在 `392`，而真实用例数已是 `589`。现在
  `i18n` 作业会比对，徽章比 `def test_` 计数还少就直接失败（参数化会让真实
  收集数高于 def 计数，所以只挡「明显过时」，不做精确相等）。

  `test` 作业加了 `timeout-minutes: 30`。套件本地实测约 400 秒，卡死时不该挂到
  GitHub 的默认 6 小时——那会白烧 9 个矩阵组合的额度，而且看不出是卡死还是在跑。

- **三份 README 的章节结构现在有测试守着。** 文案表有 `check_i18n.py` 守，
  README 一直没人守，于是三份各自演进。实测曾漂移到 zh-Hans 比另两份少 3 个
  小节标题（「网页界面」「交互菜单」「命令行」被写成加粗段落而不是 `###`）——
  后果是主语言读者的目录里没有这三项、深链失效，而 CI 全绿。

  现在断言三份的标题层级序列完全一致（26 个标题，一字不差）。只比结构不比字数：
  译文长度天然不同，按字数比对会一直误报。顺带把 zh-Hans 独有的「典型流程」
  补进了另两份。

- **网页界面在运行前明示这一次会发生什么。** 复选框的文案说的是「关掉什么」
  （「不提交，只分析」），读者得自己反推「没勾就是会提交」——而提交是不可逆的
  写操作。现在按钮上方有一行随勾选实时更新的提示，把它正过来说：本次将提交一次
  快照到 git（不可逆）、跑不跑测试、是否忽略并发写入警告。

  带写操作时左边框与文字同时加重，不靠单一颜色传达信息（色盲用户看不出颜色差，
  看得出边框与深浅）；文案本身用「不可逆」这类措辞说清严重性，不依赖粗体——
  屏幕阅读器读不出粗体，读得出「不可逆」。节点带 `role="status"` 与
  `aria-live="polite"`，勾选变化会被读屏软件念出来。

- **前端 i18n 键现在有测试覆盖了。** 此前 `app.js` 里 `t("gui.xxx")` 打错一个字、
  或 `index.html` 的 `data-i18n` 指向不存在的键，都只会在用户点到那一屏时静默
  显示 `??key??`，而 CI 全绿。新增两条静态契约：前端引用的每个键三种语言都必须
  有；前端文案不能带 HTML 标签（渲染走 `textContent`，标签会被当字面量显示）。

  扫描要认三元表达式里的键：`t(cond ? "a" : "b")` 的键不紧跟左括号。第一版正则
  只匹配 `t("`，实测漏掉了本项目 4 个这样写的键、测试假绿——所以改成先框出
  `t(...)` 的实参再取其中全部字符串字面量，按「小写点分」的形状筛。
  扫到的键从 76 个增至 98 个。

- **`AGENT_HANDOFF_CONTEXT_WINDOW`：声明模型的上下文窗口上限。** 任何单一绝对
  阈值都不可能同时对 128k 和 1M 窗口正确。实测本机一个 Claude 会话 102365
  token，按 200k 折算的兜底阈值判「健康」；若模型其实是 128k 窗口，那已经 80%、
  该判「尽快交接」；若是 1M 则只有 10%，「健康」是对的。**同一个数字，结论相反。**

  Codex 在 `token_count` 事件里直接写 `model_context_window`（实测 121600），
  所以走真实占用率；Claude Code 的转录不写窗口上限——实测本机 16 个 Claude
  转录全部读不到，只能落到绝对阈值。这个变量把答案交给知道它的人。

  优先级：**转录自写 > 用户声明 > 按 200k 折算的兜底**。转录里的是实测值，
  所以 Codex 会话不受这个变量影响。非法值（负数、零、小数、非数字）按没写处理
  ——一个笔误不该把整张体征表静默判成健康，那正是这个工具存在的意义所反对的。

  为什么不去猜模型型号：模型会换，而用户知道自己在用什么。官方也从未公布固定的
  自动压缩触发阈值，并且在「把 1M 窗口的模型按 200k 计算」这件事上出过 bug——
  可见连实现方自己都不适合硬编码这个数。

- **`--sweep`：转录占了多少磁盘、哪些可以安全回收。** 命令行、交互菜单
  （新的第 5 项）、网页界面 `/api/disk` 三处都能用，可用 `--out` 导出 md 报告。

  **它不删任何文件**，只统计、分类、排序。转录里可能存着那段工作的唯一记录，
  而删除不可逆——判断哪些真能丢需要人看一眼，所以工具给清单，由人决定。
  这与项目其余部分的立场一致：只给证据，不做不可逆动作。

  **只读文件元信息，不读内容**，这是它快的唯一原因。实测本机 423 个转录、
  1.09 GB：

  | 做法 | 耗时 |
  |---|---|
  | 只遍历目录 | 8 ms |
  | 遍历 + `stat`（本功能） | **10 ms** |
  | 读每个文件前 64 KB | 219 ms（慢 22 倍） |

  报三类可安全回收的：子代理转录（不是你参与过的对话，本来就无法续接）、
  已归档会话（Codex 里归档后续接不了，但正文还在）、空会话（不到 32 KB，
  装不下一轮有内容的对话）。三类互斥计数，一个既小又属于子代理的转录只算一次。

  **按体积排行，不按时间过期。** 实测本机「超过 30 天」是 0 个，而单个 90 MB
  的文件占了总量 8%——真正吃磁盘的永远是少数几个文件，按时间清理在这里没用。

  `--by-repo` 额外按仓库聚合，需要读转录才能拿到 cwd（实测 24 秒 vs 12 毫秒），
  所以是独立开关而非默认。它揭示的信息很直接：本机 E 盘那两个 kirara-ai
  项目占了 828 MB，是总量的 76%。

### 安全

- **会话原文里的密钥不再进交接文档。** 文档会把「用户问了什么」「最后一句输入」
  「压缩摘要」原样写进去，而人在会话里粘过 API key、`Authorization: Bearer …`、
  数据库口令是常态。此前脱敏只有两条规则（家目录、他机用户名），一条密钥正则
  都没有——凭据于是原样进文档，再被自动提交进 git。**进了 git 历史，删文件也
  去不掉，只能改写历史或轮换密钥。**

  现在 `_redact` 管道最先跑密钥脱敏，覆盖 OpenAI / Anthropic `sk-`、GitHub
  `ghp_`/`github_pat_`、Slack `xox*`、AWS `AKIA`、Google `AIza`、HTTP
  `Bearer`/`Basic` 头、PEM 私钥整块、URL 里的 `user:pass@`，以及
  `api_key: …` / `TOKEN=…` 这类键值写法（键名含密钥词根的都算）。

  **保留前缀而不是整段打码**：读者要知道「这里曾有一个 OpenAI 密钥」才知道该去
  轮换哪一个，`sk-***` 比 `***` 有用。

  **只认有明确前缀的形态，不做高熵字符串猜测**：那类启发式会把 commit sha、
  正常的 base64、长文件名一起打码。宁可漏掉自造格式的私有 token，也不能让读者
  面对一份处处是 `***` 的文档——那等于没有文档。同理，值为纯数字时一律不动，
  因为 `max_tokens=8192`、`token_count: 123` 是模型配置的常态，
  而上下文占用恰恰是本工具最核心的判据。

- **计划文档声明为用户私有的点文件，此前排除失败、会被提交进去。**
  `.lstrip("./")` 收的是**字符集合**而不是前缀，把开头所有的 `.` 和 `/` 逐个
  剥掉：`.env` 变成 `env`、`.gitignore` 变成 `gitignore`、
  `.claude/settings.json` 变成 `claude/settings.json`。名字错一个字符，
  `:(exclude,literal)` 就匹配不上真实路径，于是 `git add -A` 照收不误。
  点文件恰恰是放密钥最常见的地方。同一个 bug 在并发信号的忽略集里也有一份，
  一并修掉。

- **脱敏漏掉的字段补齐。** `_redact` 此前是逐调用点手工调用，7 处漏了：
  计划文档路径（两处）、git 报错、测试命令、测试输出摘要、环境陷阱、
  最近提交主题。它们同样带绝对路径，而绝对路径里带着用户名。

- **混合分隔符的路径此前完全不脱敏。** 每种家目录形态都整体 `re.escape`，
  只能匹配「全 `/`」或「全 `\`」；实测 `C:\Users/devin/proj` 与
  `C:/Users\devin\proj` 一条都匹配不上。这两种形态在 `os.path.join` 拼接和
  转录 JSON 里很常见。现在按分隔符切段匹配，并容忍重复分隔符
  （JSON 原文里的 `C:\\Users`）。

- **从 POSIX 壳里跑时，本机用户名全量泄漏。** Git Bash / MSYS 把 `HOME` 设成
  `/c/Users/alice`，WSL 设成 `/mnt/c/Users/alice`，而文档里的路径来自转录、
  写的是 `C:/Users/alice`——两边形态不同，一条也匹配不上。现在把同一个家目录的
  跨壳等价形态都列进脱敏表。

- **少见的家目录布局补上**：`/export/home/`、`/usr/home/`（Solaris / 部分 BSD）、
  `C:\Documents and Settings\`（旧 Windows，转录可能来自老机器）、
  `\\?\C:\Users\`（Windows 长路径前缀）。漏一条就等于那种机器上的用户名原样
  进文档。`/root` 刻意不算：它是家目录**本身**，其后是项目名而非用户名，
  当前缀处理反而会把项目名打成 `_USER_`。

### 修复

- **`## Goal` 这种写法认不出意图段落。** 交接提示词的作用之一是点名「原始要求
  写在哪一段」，否则新会话把计划当待办清单读，漏掉整体目标与红线约束——而那
  恰恰是交接最不能丢的东西。

  根因是词表被拆在两个分支里各写了一份：`Goal` 只出现在粗体分支
  （`**Goal:**`），标题分支只收 `Constraints`、`Overview`、`Background`。
  于是 `## Goal`——最常见的写法，**本工具自己的计划文档用的就是它**——
  一直认不出来，提示词照常生成但少了这一句点名。

  现在两种写法共用同一组词（`_INTENT_WORDS`），加一个词两处同时生效，不会再
  漂移。词表同时补上 `Goals`、`Objectives`、`Non-Goals`、`Scope`、`目的`、
  `范围`、`红线`。标题分支加了行尾锚定，所以 `## Goalkeeper` 与正文里的内联
  `Goal` 不会被误认——误认的代价是提示词点名一个不存在的段落，比不点名更糟。

- **同一天重跑会静默丢掉上一份交接文件。** 交接文件名只带日期
  （`2026-08-23-handoff.md`），所以第二次运行直接覆盖第一次，没有备份、
  没有确认、没有提示。实测本项目自己的 `docs/2026-08-23-handoff.md` 一天内被
  覆盖过三次，前两份只能从 git 历史里挖——若当时用了 `--no-commit`，就彻底
  没了。一个以「别丢上下文」为全部意义的工具，自己把上下文丢掉是最讽刺的失败。

  现在覆盖前先比字节：**相同就直接覆盖**（多数重跑是「调完再跑」，要的就是
  最新那份，攒一堆 `-2`、`-3` 只会让人分不清该读哪个），**不同才留一份
  `<原名>.prev.md`**。只保留最近一份——留成长链又是新的「该读哪个」问题，
  真正的历史归 git 管。备份失败只提示不阻断：用户要的是新交接文件，
  为了留旧的而不产出新的是本末倒置。

  备份路径同时加进了并发信号的排除集（`_prev_path()` 单点定义，两处共用）。
  漏掉这条排除的后果是第三次运行报冲突并建议加 `--force`，而真相是没有任何
  别的会话——工具在跟自己上一秒的产物打架。

- **detached HEAD 被报成一个叫 `HEAD` 的分支。** 交接文档写「分支：`HEAD`
  （**不是主干**）」，接续会话于是以为存在这样一个分支。

  根因是 `git rev-parse --abbrev-ref HEAD` 在 detached 状态下返回的是**字面量**
  `HEAD`——非空、退出码 0，所以原来那句 `or "<detached>"` 永远不触发，是死代码。
  实测确认：在分支上返回 `master`，detached 时返回 `HEAD`；而
  `git symbolic-ref --short -q HEAD` 在分支上返回 `master`、detached 时返回空，
  那才是 git 用来区分两者的接口。现在改用后者。

  这不只是显示不准。detached 状态下提交，提交**不属于任何分支**：切走之后只能
  靠 sha 找回，`git branch` 里看不到，reflog 过期后可能被 gc 回收。接续会话必须
  知道这件事，否则它照常提交，而那些提交随时会变成悬空对象。所以文档现在给
  detached 单独的警告并附上补救命令（`git switch -c <名字>`），不再只标
  「不是主干」——后者会让读者以为只是分支名的问题。

  `<detached>` 成了 `gitops` 的导出常量，因为下游要靠它识别这个状态。

- **`pip install` 之后图文说明打不开。** 网页界面的「指南」入口必然 404、交互
  菜单第 6 项必然打印「找不到」——而那份 104 KB 的三语文档正是本项目的主要
  说明载体。

  根因是它从来没进过 wheel：`package-data` 只收 `i18n/*.json` 与
  `gui/static/*`，`docs/` 不在其中。两处调用各自往上数层级去找源码树里的
  `docs/guide.html`（`gui/server.py` 数四层、`menu.py` 数三层——两者深度不同，
  所以层数本来就该不同，**各自都对**），但装成 wheel 后往上数几层都是
  `site-packages`，怎么数都找不到。

  现在 `scripts/build_guide.py` 在生成 `docs/guide.html` 的同时，往
  `gui/static/` 写一份副本，于是它随 wheel 分发。这份副本**不进 git**
  （已加进 `.gitignore`）：同一份内容有两个真源就会漂移，而那正是当初拒绝
  打包的理由。CI 的新鲜度检查仍只盯 `docs/guide.html` 那一份，不受影响。

  查找逻辑收拢到 `platform.find_guide()`，两处共用，顺序是**包内副本 >
  源码检出 > `AGENT_HANDOFF_HOME` 指向的检出**。找不到返回 `None` 而不抛异常
  ——图文说明缺失不影响工具的任何实际功能，为此让调用方崩掉是本末倒置。

  实测验证：在隔离 venv 里装 wheel（模拟另一台电脑），`--version`、三语
  `--help`、`--sweep` 全部正常，`find_guide()` 从
  `site-packages/agent_handoff/gui/static/guide.html` 找到 104455 字节。

- **CI 只检查 wheel 里的文件名，从不真装一次。** 上面那个缺陷正是被这个缺口
  放过的：文件名检查全绿，装完却打不开说明书——因为它压根不在检查清单里。

  现在 `build` job 多一步：建一个干净 venv 装 wheel，**在仓库外的目录**里跑
  `--version`、三语 `--help`、`--sweep`，并断言 `find_guide()` 能从装好的包里
  找到完整的说明文档。在仓库外跑是为了模拟另一台电脑，避免从检出里意外读到
  `docs/` 或 `src/`。清单也补上了 `guide.html` 与 `core/disk.py`——后者曾经
  从某个 wheel 里整个漏掉，而 README 与 CHANGELOG 通篇在讲它实现的 `--sweep`。

- **Python 3.9 兼容性没有任何自动守护。** `pyproject.toml` 声明
  `requires-python = ">=3.9"`，CI 也在 3.9 上跑，但开发机跑的是新版本——一个
  3.10+ 的 API 只要落在不常走的分支里，本地和多数 CI 组合都碰不到它，唯独真在
  3.9 上跑到那一行的用户会崩。

  这已经发生过一次：`cli.py` 用 `write_text(..., newline="\n")` 导出 `--sweep`
  报告，而 `pathlib` 的 `newline=` 是 3.10 才加的，于是
  `agent-handoff --sweep --out report.md` 在 3.9 上抛 `TypeError`。同一仓库的
  `core/handoff.py` 早就注释过这个坑并改用 `write_bytes` 绕开，`cli.py` 漏改，
  而测试套件里没有任何东西会因此变红。

  新增 `tests/test_py39_compat.py`：用 `ast` 静态扫描 `src/` 全部源码，挡掉
  `pathlib` 的 `newline=`、`match` 语句、`itertools.pairwise`，并要求用了
  `X | Y` 注解的模块必须有 `from __future__ import annotations`（3.9 上那种
  注解在运行时求值会抛 TypeError）。

  用 `ast` 而不是正则，是因为注释和字符串里的字面量会让正则误报——本文件这段
  说明里就含 `newline=`。另外锁住 `requires-python` 本身：哪天真的放弃 3.9，
  这些检查要跟着删，而不是悄悄失效。

  静态扫描而不是运行时检查：运行时只能发现「这次跑到的那条路径」，静态扫描
  覆盖全部代码。已用注入违规的方式验证过它真的会变红，不是永远通过的空壳。

- **超时的测试被报成「通过」。** 超时是把进程杀掉，被杀之前 pytest 已经打印了
  一部分结果。超时检查此前排在框架摘要之后，于是
  `"3 passed\n<timeout after 900s>"` 先命中 pytest 分支，摘要变成 `"3 passed"`
  ——交接文档据此声称测试通过，而其余用例一个都没跑。

  这是本工具最不该出的错：它存在的意义就是给出可信的实测证据，而**把超时报成
  通过比不报测试结果糟得多**——后者只是缺信息，前者是给出假信息，接续会话会
  据此认为代码是好的。现在超时最先判定，并保留「中止前已通过 N 个」这条线索
  （三语文案，不是硬编码英文）。

- **繁体中文 Windows 上界面和交接文档全变简体。** `locale.getlocale()` 在
  Windows 上返回的不是 BCP-47 标记，而是 `Chinese (Traditional)_Taiwan` 这种
  人类可读名。归一化函数只认 `en*`/`zh*` 前缀，该字符串以 `C` 开头，于是落到
  基准语言。简体机器上「恰好正确」掩盖了这个缺陷，繁中机器上必然错——而
  Windows 默认不设 `LANG`，所以这是繁中用户的默认遭遇。

- **`LANG=C.UTF-8` 的英文环境拿到简体中文输出。** POSIX 里 `C` 与 `POSIX` 的
  含义是「不做本地化」，不是某种语言，而容器、CI、cron、纯 SSH 会话默认就是它。
  现在把它视为「无语言偏好」并继续往下探测。

- **`LANGUAGE=fr:en` 落到简体中文。** 它是冒号分隔的**优先级列表**
  （「法语优先、英语次选」），此前只取第一段就返回。现在逐段试，
  第一个能认出的受支持语言胜出——与 gettext 惯例一致。

- **菜单的确认词写死在代码里。** `("y","yes","是","好")` 只覆盖英文与简体；
  繁中用户输入「要」「確定」会落到 else 分支，`--by-repo` 静默不生效，
  用户以为功能坏了。现在确认词来自文案表，每种语言可按自己的习惯补多个说法，
  英文 `y`/`yes` 始终接受（终端交互的通用惯例）。

  这处能长期逃过 CI 是因为 `scripts/check_i18n.py` 的白名单以**整个文件**为
  粒度：一条「旧版说明文件名」的理由把 `menu.py` 全文的中文字面量检查都关掉了。

- **`--sweep --out` 在 Python 3.9 上直接崩溃。** `pathlib` 的 `newline=` 参数
  是 3.10 才有的，而项目声明支持 3.9（CI 也跑 3.9 矩阵）。改为写字节并把换行
  钉成 LF——`core/handoff.py` 早就为同一个坑这么做了，这里补齐。


  判据**不猜用户名**（硬编码名单换台机器就失效），而是看路径里的家目录段：
  `<盘符>:\Users\<名字>`、`/home/<名字>`、`/Users/<名字>`、`/mnt/<盘>/Users/<名字>`
  这四种形状在所有机器上都成立，其中的名字不是本机任何一个（`~` 的目录名，
  加上 `USERNAME`/`USER`/`LOGNAME`——WSL 下 Linux 侧与 Windows 侧的名字可能
  不同，两个都算本机）就是外来。

  **刻意不把「路径不存在」算作外来**：本机删掉一个项目目录是常事，而外来判断
  会关掉原生续接——那个会话在应用索引里还在，续接本来可用，误判等于把能用的
  功能关掉。这一点是实施过程中被既有测试挡住才发现的。路径失效由独立的
  `path_is_stale` 表达，两者后果不同：一个说「续接不了」，一个只说「这条路径
  打不开」。

- **迁机时反而把别人的用户名写进了交接文档。** `_redact_home` 只认**本机**
  家目录，对 `D:\Users\bob\...` 一条规则都匹配不上。新增 `_redact_foreign_users`
  把别人的用户名换成 `_USER_`，只换名字那一段、保留其余路径（读者仍要靠路径
  判断「那台机器上的项目在哪」）。

  两种形态都处理，实测漏掉后者会让用户名从转录路径漏出去：真实路径，以及
  Claude 的 slug 目录名 `D--Users-bob-myproj`——它把 cwd 里的非字母数字全换成
  `-`，用户名嵌在其中一段里，认不出真实分隔符。本机名字仍由 `_redact_home`
  处理成 `~`，外来规则跳过它，同一个名字不会被换两次。

- **仓库路径本身从未脱敏**（`doc.scene.repo`）。它是本机路径、读者就在本机用，
  但这份文档会进 git、可能推到公开仓库——受众不同。现在写成 `~/proj`，
  在 shell 与两个应用里都会展开，脱敏后仍可直接使用。

- **外来会话的「先 git init」建议是错的**——那个目录本机根本没有。改成
  「指向本机对应的仓库，或直接把这个会话的内容交接过来」。

- 交接文档为外来会话加一行明确说明：下面的工作目录、涉及仓库、转录位置都是
  那台机器上的路径，本机解析不出来，也无法原生续接；**内容依然有效，位置不再
  有效**。命令行卡片与网页界面（警示色徽章 + 悬停说明）同样标注。

  为什么是标注而不是隐藏：外来转录的**内容恰恰是迁机时最想带走的东西**，
  token 判据也与机器无关（实测 160,000 token 在任何机器上都判「尽快交接」）。
  失效的只有位置。

- **数据目录被环境变量挪走时，一个转录都找不到。** 两个应用都支持
  `CLAUDE_CONFIG_DIR` / `CODEX_HOME` 改位置（笔记本 C 盘小、把它指向 D 盘
  是常见做法），而代码只认家目录下的 `.claude` / `.codex`。现在这两个变量
  优先于家目录，家目录仍作兜底保留（两处都可能有历史记录，丢一边就是丢会话）。
  空值与用户复制粘贴常带的引号都会被正确忽略。

  顺带确认一件容易误解的事：**转录不会因为项目在 D/E/F 盘而跟着搬家。**
  实测本机 E 盘上的项目，转录仍在 C 盘家目录下，盘符只出现在转录内容记录的
  `cwd` 里。会变的是数据目录本身，不是「每个项目各存一处」。

### 变更（判据）

- **每张会话卡片给出原生续接命令**（`claude --resume <id>` / `codex resume <uuid>`），
  命令行显示、网页界面一键复制。**原生续接严格优于交接**：工具授权、后台进程、
  被否决方案的推理过程，交接一样都带不走。工具原先判出「立刻交接」却不提这条
  更好的路，等于把用户往有损的方案上推。

  Codex 的 UUID 取会话 ID 尾部五段而不是写正则——rollout 文件名前缀自带很多
  连字符，尾部五段无论前缀怎么变都对（这个取法来自 codex-history-vscode）。
  **归档过的 Codex 会话不给命令**：Codex 只在活动目录里找，给一条注定失败的
  命令比不给更糟。

- **网页界面接上了图文说明入口。** `gui.nav.guide` 等三个文案键早就存在，
  但前端一次都没引用——文案在、功能不在。说明文档由单独一条路由送
  （它是仓库文档、会随三份 JSON 重新生成，复制进包只会让两边漂移），
  链接带 `#lang=` 跟随界面语言，装好的包里没有 `docs/` 时自动隐藏入口。

### 变更（判据）

- **风险判定改看上下文占用，不再拿文件体积当主判据。** 占用数本来就写在
  转录里，没有理由去猜：Claude 记在 `message.usage`（`input_tokens` 加两种
  cache 读入），Codex 的 `token_count` 事件还额外带 `model_context_window`，
  所以 Codex 侧能算出真占用率，Claude 侧则拿占用量对照按 200k 窗口折算的
  阈值（对更大的窗口偏保守，宁可早提醒）。

  为什么必须改——本机实测四个转录，体积与真实占用严重脱钩：

  | 转录 | 体积 | 实际占用 | 压缩 | 旧判定 | 新判定 |
  |---|---|---|---|---|---|
  | A | 1.0 MB | 194,183 token | 0 | 健康 | **立刻交接** |
  | B | 2.0 MB | 172,490 token | **10 次** | 留意 | **立刻交接** |
  | C | 27.4 MB | 710,340 token | 0 | 立刻交接 | 立刻交接 |
  | D（Codex） | 6.4 MB | 121,407 / 121,600 = **99.8%** | 1 次 | 尽快交接 | **立刻交接** |

  B 最能说明问题：**它体积小恰恰因为压缩一直在丢历史**，越小可能丢得越多，
  而旧判据把这种最该交接的会话标成「留意」。占用取峰值而非末值——末条
  assistant 消息可能来自子代理或没有 cache 记账，实测两个转录的末值都是 0。

- **压缩过的会话直接按「满过」对待。** 自动压缩只在上下文装不下时才触发，
  所以它是最硬的证据，比任何百分比都清楚。压缩一次即「尽快交接」，两次以上
  「立刻交接」。Claude 的压缩事件在 `type:"system"` + `subtype:"compact_boundary"`
  里（带 `compactMetadata.preTokens`），Codex 是独立的 `compacted` 记录。

- **体积降级为兜底判据**，只在转录里读不到 token 数时使用（旧格式、被截断的
  文件、非深度扫描）。原先那套实测分档仍然保留，因为那时它是唯一可用的信号。

- **同区间内改按占用排序**，不再按体积。一个压缩过 10 次的 2 MB 会话比一个
  从没压缩的 20 MB 会话更紧急，而体积排序会把它压到后面。

- 命令行卡片、网页界面与交接文档都显示占用（占用率 / 占用量 / 压缩次数三种
  措辞，对应三种可信程度），并排在体积**之前**——判据是什么就该先看到什么。
  网页界面的 tooltip 直接说明「判定依据是这个，不是文件体积」。

- 三份 README 与图文说明里所有「体积越大越危险」的表述一并改写。文档与代码
  必须说同一件事，否则读者会按错的判据决策。

### 修复

- **`cli.vitals.worst` 把风险区间输出成原始英文键名**（`critical`），
  三种语言下都显示英文。同一行还残留 `file[:14]` 截断——Codex 转录全被截成
  同一个 `rollout-2026-0`，指不到任何具体文件；改用会话 ID 前 8 位。

- **交接文档里的会话体征表在 GitHub 上不渲染成表格。** 分隔行的竖线数写死成
  6 个，而表头是 7 列（智能体 / 会话 / 体积 / 致命 / 中断 / 工具错误 / 判定）。
  Markdown 要求两者相等，否则整块退化成普通文本——交接文档最该一眼看清的
  那张表反而没了表格。改成从表头推算列数，文案改列时不会再失配。
  原先的测试只断言「表头这句话在不在文档里」，所以漏掉了整类缺陷；
  现在按三种语言分别校验每张表的表头与分隔行列数一致。

- **压缩窗口分隔标题把中文写进了英文交接文档。** 摘要拼接发生在纯解析层，
  那里拿不到语言对象，于是分隔行写死成 `===== 压缩窗口 1 / 3 =====`，
  跟着摘要进了 `--lang en` 的产出。改成不含任何自然语言的 `===== [1/3] =====`，
  读取侧改用配对的正则识别（原先靠中文前缀判断，换写法就会把分隔行当正文）。

- **切换界面语言会清空正在运行的任务日志。** 重绘时日志的唯一副本在 DOM 里，
  被重绘本身抹掉；现在单独留一份，切语言后日志继续接着长。

- **本机服务中断时网页界面永远转圈。** 轮询失败被静默忽略，进度条不停、
  也不报错。现在容忍几次抖动后明确告知失去联系，并提示交接可能其实已经完成，
  重跑前先去看仓库和交接文件。

- **记住的语言会盖掉本次明确指定的语言。** 主题早就是「显式优先于记住的」，
  语言却不是：`?lang=` 已经让服务端渲染了对应语言，前端又用 localStorage
  的旧值覆盖回去。现在与主题一致，并让 `#lang=` 同样能压过记住的值。

- **英文与繁中用户点开说明文档只会看到简体中文。** 菜单按 `guide.<语言>.html`
  找文件，而构建脚本只产出一个 `guide.html`（三语文案都在里面、由前端挑），
  那种文件名从来不存在，于是必然落到简体那一档。改为传 `#lang=`；
  说明文档也开始接受 `?lang=` / `#lang=`，原先它只按浏览器语言猜。

- **英文输出里出现全角分号。** 勾选会话后的回显用 `；` 连接标题，
  与语言无关地写死。现在按语言选分隔符。

- **完成度表的列名靠切分 Markdown 表头文案得到**，文案改列序就会错位，
  而第一列还漏成了写死的英文。改成各列自己的文案键。

- 无障碍标签（语言、主题两个切换组）过去永不翻译，屏幕阅读器用户只听得到
  英文；现在跟随界面语言。

- 会话卡片的话题用了一个从未定义的颜色变量，实际继承上层颜色，
  比注释所述的「最醒目」反而更弱。

- **双击运行时若没装 Python，只会看到 cmd.exe 那句 `'py' 不是内部或外部命令`。**
  更糟的一种情形：微软商店预置了一个名叫 `python.exe` 的占位程序，
  `where python` 能查到它，于是启动器认为找到了解释器，实际点开的是商店页面，
  而且返回码可能是 0——窗口连 `pause` 都不给就消失了。改成**用运行来探测**
  （真解释器能执行 `import sys` 并返回 0，占位程序不能），找不到时给出
  下载地址与「勾选 Add python.exe to PATH」的具体指引，并明确说明商店占位程序
  不算。同时增加版本下限检查：3.9 以下直接拒绝，而不是让用户面对包内部抛出的
  `SyntaxError` 堆栈。两个 `.sh` 启动器同样补上这两道检查。

- **双击运行的窗口在正常结束时会瞬间关闭**（原先只在 `errorlevel 1` 才 `pause`），
  跑完什么都没留下。现在总是暂停。

- `agent-handoff.cmd` 缺 `chcp 65001`：这个工具三种语言里两种是中文，
  默认 GBK 控制台下输出是乱码。

### 变更

- 「全选」的说法改由文案表提供（`cli.pick.all_words`）。原先
  `("a", "all", "全部", "全选")` 写死在代码里：英文界面下莫名接受中文输入，
  而任何新增语言都拿不到自己的说法。`a` / `all` 仍然始终接受——提示语里
  写的就是它们。

- 语言切换钮的短标签（简 / 繁 / EN）移到文案模块与语言名并列。这些标签
  **按设计各用自己的语言写**，不该翻译，但写死在前端会让新增语言静默显示成
  `EN`。

- CI 的 lint 覆盖 `scripts/`。那里的 `build_guide.py` 与 `check_i18n.py`
  正是 i18n 与说明文档两道门所依赖的工具，它们坏掉会让门失效，却因为不在
  `src/` 下而从不被检查。

- `check_i18n.py` 增加第四项检查：源码里不得有绕过文案表的中文字面量。
  用 Python 分词器区分注释、文档字符串与真正的字符串字面量——注释里的中文
  是解释代码用的，完全正当。确属输入侧模式（解析中文计划文档、识别中文错误
  标记）或语言自称的，列入白名单并写明理由。本轮的全角分号与压缩窗口标题
  两处泄漏就是这道门原先抓不到的类型。

- **端到端测试增加 Windows 作业。** Windows 是这个工具的主要使用环境
  （转录默认就在 `%USERPROFILE%` 下），但端到端此前只在 Linux 上跑，
  于是 Windows venv 布局（`Scripts\` 而不是 `bin/`）、控制台编码、
  启动器脚本全都没有端到端覆盖。新作业在真 Windows 上建仓库、跑一次交接，
  校验复选框回填、受保护文件不外泄、交接文件仍是纯 LF、Windows venv
  布局被识别、提示词点名意图段落，并实际调用一次 `agent-handoff.cmd`。
  这八条断言都先在本机 Windows 上验证过才写进 CI。

## [2.3.0] - 2026-08-21

计划解析器与风险判据的精度修复。这一版的每条改动都对应一个**实测复现**的
误判——不是「加个 feature」，是原先会把真实情况读错。

### 修复

- **计划解析器认得真实 markdown 写法。** 原先 6 类常见写法被整条丢弃，
  后果不是报错，而是静默判成「该任务没有文件 / 没有符号 / 没有步骤」，
  完成度随之失真：
  - 文件行：补 `- **Modify**: x`（粗体动词）、`- Modify x`（无冒号）、
    `- \`x\` — 说明`（无动词）、`Delete/Remove/Rename` 动词，以及**一行多路径**
    （`- Create: \`a.ts\`, \`b.ts\`` 原先只取第一个，file_ratio 分母偏小、
    缺失文件漏报、完成度偏乐观）。放宽后加了散文护栏：没有反引号时要求
    路径含 `/` 或扩展名，`- 这一步不涉及任何文件` 不会被当成路径。
  - 前导斜杠路径归一化：`- Modify: /webui/src/x.ts` 原先经 `repo / path`
    会跳到盘符根（仓库之外），文件判定跑错位置、永远判缺失。
  - 复选框：补 CommonMark 的 `*` 与 `+` 列表符、双空格、`[~]`。原先只认 `-`，
    用 `*` 组织的计划整份复选框消失，连 `find_plan` 都会因「复选框少于 3 个」
    拒绝该文档。`[~]`（部分完成）按未完成算——勾选不可逆，宁可少勾。
  - 任务标题：`#{1,6}` + 允许缩进 + 认 `Phase`/`阶段`。原先卡死 `#{2,4}` 且
    要求行首无空格，`##### Task 5` 与缩进标题被整段忽略。
  - 步骤号：粗体可选，`- [ ] Step 1: x` 原先不生成 Step，进度显示成 0 / 0。
  - Interfaces 段：补 `Exports`/`Provides`/`Returns`/`提供`/`导出` 等声明动词，
    原先只认 `Produces`/`产出`。符号名长度下限从 `> 3` 放到 `>= 2`——
    原先 `run` `add` `get` `id` 这些真实接口名被静默丢弃。
- **致命错误签名不再把「讨论」当「发生」。** 原先在整行原文上裸匹配，实测
  本机 14 个主转录的 239 个命中里 94 个来自 assistant 正文、83 个来自 user
  正文——那是人和模型在讨论这些词（用户 CLAUDE.md 里就写着「出现
  content-blocked、熔断时…」）。于是 17/24 个转录被标 `fatal>0`，其中 16 个
  体积完全健康，风险列变成纯噪声。现在锚定在错误载荷字段上
  （`"error"`/`"error_message"`/`"reason"` 等的值里），Claude 侧 37 个
  幽灵 fatal 归零，而 Codex 侧真实的 HTTP 503「所有供应商已熔断，无可用渠道」
  仍被正确计入。
- **风险分档纳入失败信号。** 原先只看体积：0.9 MB 但撞过熔断的会话被标
  「健康」，1.7 MB 一切正常的被标「留意」，用户照徽章决策会看错优先级。
  体积衡量「还能撑多久」，fatal/aborted 衡量「已经出事了没有」，两者都进判定。
- **体征表的转录标识不再全都一样。** 原先用 `file[:14]`，Codex 文件名都以
  `rollout-2026-0…` 开头，实测 12 行截成同一个字符串；`doc.vitals.worst`
  也用同一截断，「最紧迫的是 rollout-2026-0」指不到任何文件。改用会话 ID 前 8 位。

### 新增

- **统计并显示被打断的轮次**（`turn_aborted` / `reason: interrupted`）。
  实测 Codex 侧 `is_error` 在 40 个 rollout 里只有 3 次，而 `turn_aborted`
  有 6 次——后者才是「这轮没做完」的真实信号。把半成品当成已完成是交接里
  最贵的误判，所以它在卡片、体征表与网页界面里都单独成列。

### 安全

- **交接文档不再泄露用户名。** 家目录在文档里被替换成 `~` / `_HOME_`，
  覆盖原生分隔符、正斜杠、反斜杠以及 Claude Code 的目录名变体
  （`C--Users-alice`），大小写不敏感。文档里嵌的那份提示词同样脱敏——
  它是要贴到别处去的，漏在那儿等于漏两次。

### 其它修复（2.3.0 发布后陆续修的，一并记在此版下）

- **git 变成可选。** 没装 git 或目标不是仓库时，不再直接失败退出：
  提交快照与最近提交自然缺席，会话传承与完成度评分照常工作，
  文档里明确写出「不在 git 之下」而不是留空让人误以为干净。
- **归档的 Codex 会话找得到了。** 只扫 `sessions/`，漏掉
  `archived_sessions/`——那里恰恰是已经结束、最需要被传承的会话。
- **符号检索看得见尚未提交的文件。** 原先只查已提交内容，刚写完还没
  `git add` 的实现会被判成「符号不存在」，完成度偏悲观。
- **扫描缓存有了上界**，长时间运行不再无限增长；仓库相关性判断复用同一套
  判据，不再两处各写一遍互相漂移。
- 说明文档重做成仪表盘式，加入实测判据与「什么时候不该用这个工具」；
  截图改由脚本确定性生成，不再带出本机用户名。

## [2.2.0] - 2026-08-21

2.1.0 的会话传承**有损**，而且损失量大到会改变结论。这一版修掉它，并把
「哪些东西原理上传不过去」写进提示词——不声明这一点，接续会话会把
「摘要里没有」当成「没发生过」。

### 修复

- **压缩摘要不再只保留最后一个窗口。** 2.1.0 假设「后一次压缩累积了之前所有
  窗口」，该假设是错的。实测本机 70 个带压缩的 rollout（52 个多窗口，最多
  19 个窗口）：`window_number` 递增、`previous_window_id` 串成链，每个窗口
  只总结它自己那一段，**没有任何样本**的末窗逐字包含首窗。只留最后一个会丢掉
  中位 **78%**、p90 96% 的具体事实（commit sha、文件路径、测试计数），其中
  11 个 rollout 的「用户目标 / 红线约束」只出现在早期窗口——恰恰是最不能丢的
  部分。现在保留全部窗口并按时间顺序拼接，窗口之间加分隔标题（多个窗口各自
  都有「## 当前进度」，不分隔会互相矛盾）。修复后实测丢失率 0%（70/70）。
- **摘要不再截断到 4000 字符。** 实测 62/70 个末窗超过该长度，中位被切掉
  2925 字符、最多 13241 字符；截断点落在哪完全取决于模型当时怎么分段，
  切掉的往往正是结论与待办。交接文档是文件，多几十 KB 无所谓；真正有上限的
  是提示词，而提示词里只放话题与路径。
- **保留用户原话。** 压缩摘要是模型的转述，转述会丢措辞里的约束
  （「不要删除项目 A」「不要强制推送」）。`compacted` 事件的
  `replacement_history` 里保留着被摘要替换掉的原始 user 消息，逐字可用——
  实测某个 rollout 的「将项目 B 推送到 GitHub 并创建 tag」只存在于这里，
  任何摘要都没有逐字保留它。这些原话在交接文档里单独成节，并注明优先于转述。
- **未推送提交的判定不再依赖过期的远程跟踪引用。** 原先用 `@{u}..HEAD` 计数，
  它在 FETCH_HEAD 陈旧时偏小——实测项目 A 的 `ahead` 显示 0，而它的 HEAD
  在任何远程上都不存在。改用 `rev-list --count HEAD --not --remotes`；
  没有任何远程时全部提交都算未推送（一个都传不出去）。

### 新增

- **提示词给出可移植的仓库身份**：remote URL + 完整 40 位 sha。原先只写
  `E:/output/...` 这样的本机路径，新会话在另一台机器、容器、WSL 或
  Codespaces 里打开时无从定位；同一个 remote 下的两个工作副本也只能靠路径
  区分。没有远程时明确声明「只存在于本机，接续必须在同一台机器上」。
- **提示词声明未推送提交数**，避免新会话以为远程已有这些改动。
- **提示词声明交接是有损的**：工具授权、后台进程、MCP 连接、被否决方案的
  推理过程都不会传过来，「摘要里没提到」不等于「没发生过」。
- 交接文档标注每个会话的压缩窗口数，让人能判断覆盖范围。

## [2.1.0] - 2026-08-21

这一版解决一个结构性缺陷：**会话的实际工作内容此前从不进入交接**。
工具只统计转录的体积与错误计数，会话里做出的判断、验证过的结论、写下的
交接摘要，在「转录 → 交接文档」这一跳就被丢弃。新会话拿到的提示词只有
git 现场，不知道前面几十轮讨论过什么，于是重新推导，甚至推出相反的结论。

### 新增

- **勾选要传承的会话。** 扫描列出会话后，用编号选出与这个仓库相关的那些；
  它们的完整摘要写进交接文档的「前序会话」一节，开场提示词点名它们
  （话题、会话 ID、转录路径），并明确要求新会话不要重新推导已有结论。
  - 命令行：`--pick-sessions` 交互勾选，`--sessions PATH` 直接指定（可重复、
    可逗号分隔，供脚本使用）。
  - 交互菜单：「交接」与「快速交接」在真正执行那一遍自动进入勾选（预演那一遍
    不打断阅读）。
  - 网页界面：会话卡片上的复选框，工具栏常驻已选计数。
- **提取会话的实际内容**，三种来源都是会话自己写下的原始记录，不是本工具的推断：
  - Codex 的 `compacted` 事件——上下文压缩时模型写下的交接摘要，实测每份
    4000–7800 字符，含仓库路径、真实 HEAD、已读文档、验证证据、待办与红线。
  - Claude Code 的 `ai-title`——一句话话题摘要。
  - Claude Code 的 `last-prompt`——会话停在哪句用户输入上。
- **会话卡片显示「话题」**，位置最显眼。会话 ID 的前八位对人没有意义，
  而开场提问在斜杠命令回显时对所有会话都一样；话题来自上面两种摘要。
- 摘要与话题也参与 `--find` 的关键词匹配：用户记得的往往是
  「那次改工作流撤销的对话」，不是 ID 前缀。

### 修复

- **符号检测漏掉现代 TS / JS 的三大类定义写法。** 原先要求「定义关键字 +
  空格 + 符号名」，于是 `undo: () => void`（interface 成员与对象字面量属性）
  与 `performActionWithoutHistory(action) {`（方法简写）一概判为未定义。
  一个写满这些写法的前端仓库会被判成「符号全缺」，接续会话据此重做已经
  完成的工作。现在识别三种形态，并靠「符号处在声明位置」区分定义与调用，
  所以 `intent.undo()` 这类调用仍然不算定义。
- **注释与字符串里对符号的引用不再被当成定义。** `// interface undo 由 store
  提供` 会命中原先的关键字前缀规则。假阳性比假阴性更危险：它让完成度回填
  勾掉从未实现的步骤，待办从计划文档里永久消失。判定前先把注释与单行字符串
  挖空（替换成等长空白，保留行列位置）。
- **计划文档不再自己满足自己。** 计划文档里写着 ``- Produces `undo` ``，
  全库检索会搜到它，于是「宣称要做」变成「已经做完」的证据。现在把计划文档
  排除在符号检索之外。
- **检索失败不再伪装成「符号不存在」。** 三条后端全部失败时每个符号都是
  「未找到」，与「查过、确实没有」无法区分，报告会写成「未开始」。现在带上
  可信度：检索不可信时不允许判定完成，也就不会勾掉任何步骤。
- **仓库推断不再被中文全角标点截断。** 用户几乎总是写
  「仓库 E:/output/proj，分支 main」，而全角逗号不是空白字符，会被连同后面的
  中文一起吃进候选路径，`nearest_repo` 于是去找一个不存在的目录。
  实测本机 5/6 个最新 Codex 转录的「涉及仓库」因此为空。
- **子代理转录不再挤占会话列表。** Claude Code 把子代理写进
  `<会话id>/subagents/`，它们数量远超主会话（实测最新 12 个文件里 7 个是
  子代理），按修改时间混排时几乎总在前面，`--limit` 被它们吃满，用户要找的
  主会话被挤出列表。现在默认只列主会话。
- **`sessions_for_repo` 对 Codex 不再恒返回空。** 它原先只比对 `cwd`，而
  Codex Desktop 的 cwd 是 `Documents/Codex/<日期>/<slug>` 任务沙箱，实测
  20/20 个 rollout 都解析不到任何仓库。现在同时比对从会话正文里捞出、
  且经 `nearest_repo` 验证过的仓库路径。
- 开场提问不再被 `<local-command-caveat>` / `<command-name>` 这类斜杠命令
  回显的样板文字占据（实测 20 个主转录里 4 个如此）。

## [2.0.0] - 2026-08-19

从单文件脚本重写为可安装的跨平台项目。**命令行参数、语义与退出码与 1.x 完全一致**，
环境变量（`PYTHONUTF8`、`PYTHONIOENCODING`）与全部原始注释保留。

### 新增

- **本地网页界面**（`agent-handoff --gui`）。浅色为默认，深色与跟随系统可选。
  只绑定 `127.0.0.1`，每个请求校验一次性令牌，并检查 Origin/Host 以挡 DNS rebinding——
  它能对任意路径执行 `git commit`，所以这些不是可选项。
- **三种语言**：简体中文、繁體中文、English。界面、命令行输出、**以及生成的交接文档
  与开场提示词**全部跟随语言。`--lang` 指定，或 `AGENT_HANDOFF_LANG` 环境变量，
  或跟随系统区域设置。
- **会话按 APP 分组**，组内最近活动在前。组的顺序也按该组最近活动排。
- **交互菜单支持语言切换**，选择会被记住（`~/.config/agent-handoff/lang`）。
- `--json` 输出结构化结果供脚本消费。
- `--jobs` 控制并行度，`--port` / `--no-browser` 控制网页界面。
- 单文件图文说明 `docs/guide.html`，三种语言内嵌、零外链、双击即可打开。
- POSIX 启动脚本 `scripts/agent-handoff.sh` 与 `scripts/run.sh`。
- 测试摘要新增识别 jest、cargo、go；原有 pytest 与 vitest 的识别更细
  （区分 failed / error / skipped，并去重失败标识符）。
- 报告脏子模块：父仓库的提交不带子模块内部改动，接续会话会看到不一致的树。

### 修复

- **受保护文件的排除不再静默失效。** 原先用 `:!path` 简写，它在旧版 git 上不被支持，
  且当路径含 `[`、`*` 时会被当作通配符——排除失败，用户私有文件被提交上去。
  改用 `:(exclude,literal)` 长写法。
- **计划文档的换行风格不再被破坏。** 原先读 CRLF、写 LF，回填一个复选框会让
  git diff 显示整份文件都变了，接续会话完全看不出真正的改动是什么。
- **空仓库不再被误判。** 原先 `git()` 对「命令失败」和「输出为空」都返回空串，
  没有提交的仓库里 `rev-parse HEAD` 两种情况分不开。
- **Linux / macOS 上能推断出仓库了。** 原先路径正则只匹配 `C:\...` 形态；
  预筛白名单也漏掉 `/tmp`、`/workspace`、`/nix` 等前缀，改成结构判断。
- **并发检测不再误报。** 构建产物、缓存、工具自己上一轮的输出、以及受保护文件
  的改动都不再被当成「另一个会话在写」。信号分成阻断与提示两级：
  「两分钟内有文件被改」最常见的成因是用户自己刚改完，当作阻断会逼人每次都加
  `--force`，而 `--force` 会连真正的阻断信号一起放过。
- **提示词的两句话之间不再粘连**（`1 left.Do not redo Task 1.`）。
- **`**Files:` 段的结束判断**改用去缩进后的行，缩进过的 `  **Constraints:**`
  不再让后续文件被算进上一个任务。
- **整个流程共用一个时间戳。** 原先调用六次 `datetime.now()`，跨午夜运行时
  文件名的日期与文档标题的日期会不一致。
- **stderr 也钉到 UTF-8。** 原先只处理 stdout，GBK 控制台下中文报错会触发
  `UnicodeEncodeError`——错误信息把错误本身吃掉了。
- 含空格的解释器路径正确加引号（`C:\Program Files\...\python.exe` 原先被
  shell 拆成两个参数）。
- venv 布局三种都认：`Scripts/python.exe`、`bin/python`、`bin/python3`。
- 超时的命令保留已产出的部分输出，那部分往往正是失败原因所在。
- `os.startfile` 只有 Windows 有；macOS 走 `open`，Linux 走
  `xdg-open` / `gio` / `wslview`，无桌面环境时退回 `webbrowser`。
- `summarize_test_output` 用上了原先收下却从未使用的 `name` 参数。
- `print_session_card` 里 `extra` 变量的遮蔽。

### 性能

| 环节 | 1.x | 2.0 |
|---|---|---|
| 符号检索 | 每符号一次 `rg` 进程（12 任务 × 6 符号 = 72 次） | 合成一条交替正则，1 次调用 |
| 转录扫描 | 每个文件读 3 遍 | 1 遍流式；深度信息拿全后走廉价分支 |
| 多个转录 | 串行 | 线程池并行 + mtime/size 缓存 |
| 定位计划文档 | 每个 `.md` 读 60 KB | 体积初筛 + 分段读，只有候选付全额代价 |
| 测试命令 | 串行 | 互不依赖的命令并行 |

### 工程

- 拆成可导入的包，CLI 与网页界面共用 `core/`，两个前端不可能行为漂移。
- 294 项测试，Windows 与 Linux（Python 3.9 / 3.11）均通过。
- CI 覆盖 Linux / Windows / macOS × Python 3.9–3.13，另有三语文案对齐校验、
  端到端验证、以及说明文档新鲜度检查。
- 零第三方运行时依赖：这个工具在环境已经出问题之后才被运行，任何需要
  `pip install` 的东西都是一条新的失败路径。

[2.3.0]: https://github.com/HyskoaMorroh/agent-handoff/releases/tag/v2.3.0
[2.2.0]: https://github.com/HyskoaMorroh/agent-handoff/releases/tag/v2.2.0
[2.1.0]: https://github.com/HyskoaMorroh/agent-handoff/releases/tag/v2.1.0
[2.0.0]: https://github.com/HyskoaMorroh/agent-handoff/releases/tag/v2.0.0
